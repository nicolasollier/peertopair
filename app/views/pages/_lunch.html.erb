<div class="lunch" style="display: none;">
  <%= simple_form_for(@event) do |f| %>
    <%= f.input :start_date, as: :datetime, html5: true, label: false, input_html: { id: "event-date" } %>
    <%= f.submit class: 'btn rounded-pill sign-up-btn mb-5' %>
  <% end %>
</div>
